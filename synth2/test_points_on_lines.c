#include <stdio.h>
#include "util.h"
/*
int main(int argc, char** argv) {
    // flipping axis is same result, still fine if its just on an axis
    float d_point_in_middle = dist_point_line_seg(0.5, 0, 0, 0, 1, 0);
    printf("point in middle dist: %f\n", d_point_in_middle);

    float d2 = dist_point_line_seg(0.5, 1.2, 0, 0, 1, 0);
    printf("point out from middle dist: %f\n", d2);

    float d3 = dist_point_line_seg(-0.7, 0, 0, 0, 0, 1);
    printf("point off end dist: %f\n", d3);

    float d4 = dist_point_line_seg(1, 1, 0, 0, 2, 2);
    printf("point in middle dist: %f\n", d4);

    float d5 = dist_point_line_seg(0, 1, 0, 0, 2, 2);
    printf("point out from middle dist: %f\n", d5);

    float d6 = dist_point_line_seg(-1, -1, 0, 0, 2, 2);
    printf("point off end dist: %f\n", d6);
}
*/